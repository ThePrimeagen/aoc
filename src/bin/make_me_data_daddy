#!/usr/bin/env bash
# i never learn with my naming conventions
# i love you mom

counter=1
while [[ $counter -lt 300000 ]]; do
    cat out >> long
    counter=$(($counter + 1))
done

cat end >> long;

# i get it, i suck at this crap
tr -d "\n\r " < long > l2
rm long
mv l2 long
